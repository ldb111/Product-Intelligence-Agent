# Product Intelligence Agent｜Codex 项目开发规范

## 1. 代码注释规范

本项目代码需要方便 Python（编程语言）初学者在 PyCharm（Python 集成开发环境）中阅读、理解和学习。

生成或修改代码时：

- 关键注释使用自然中文。
- 每个核心函数需要说明：
  - 这个函数在整个业务链路中负责什么；
  - 输入是什么；
  - 主要处理过程是什么；
  - 输出是什么。
- 对不容易理解的代码块，重点解释“为什么这样做”，而不只是把代码翻译成中文。
- 对以下类型的代码，应根据实际情况增加清晰中文注释：
  - HTTP（超文本传输协议）请求；
  - HTTP Status Code（HTTP 状态码）判断；
  - URL（网页地址）校验；
  - Exception Handling（异常处理）；
  - 网页解析；
  - JSON（结构化数据格式）转换；
  - LLM（大语言模型）调用；
  - Structured Output（结构化输出）；
  - 数据库存取；
  - State（状态）比较；
  - Evaluation（评测）；
  - Agent Tool（智能体工具）调用；
  - 其他初学者不容易理解的实现逻辑。
- 如果代码实现了 Product Intelligence Agent 的重要业务规则，需要在对应位置解释为什么存在这个规则。
- 如果当前实现存在已知限制，应在相关代码附近用中文说明。
- 不要只写一句非常简短的英文 Docstring（文档字符串）作为主要解释。
- 不要对变量赋值、`print`、简单 `return` 等明显代码进行逐行冗余注释。
- 注释目标是帮助初学者理解“代码结构 + 技术逻辑 + 业务原因”，而不是追求注释数量。

## 2. 开发范围控制

- 每次只完成当前 Task（任务）明确要求的范围。
- 不得自行扩展未要求的产品功能。
- 不得为了“架构完整”提前引入当前阶段不需要的复杂组件或框架。
- 如果发现当前方案存在重要限制或需要新增技术方案，应先说明问题，不要自行扩大实现范围。

## 3. 产品逻辑优先

- 编码前先理解当前 Product Intelligence Agent 的业务规则。
- 技术实现不得偷偷改变已经确定的产品语义。
- 如果技术实现与业务规则存在冲突，应明确指出，而不是自行选择一种理解。

## 4. 任务完成规则

Codex 声称“已完成”不代表 Task（任务）已经正式 Done（完成）。

代码任务最终仍需经过：

1. 代码实现；
2. 自动化测试（如适用）；
3. 用户本人实际运行；
4. 验收标准检查；
5. 用户能够解释核心代码；
6. 记录真实问题和限制；
7. Git Commit（Git 代码提交）。

## 5. 修改原则

- 优先进行最小必要修改。
- 不因局部问题重写无关模块。
- 不未经说明新增大量依赖。
- 新增依赖时说明其用途和必要性。
- 保持代码结构清晰，避免为了体现“专业”而过度抽象。
